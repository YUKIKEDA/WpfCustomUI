<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="Wcu.ProgressBar" TargetType="{x:Type ProgressBar}">
        <Setter Property="Background" Value="{DynamicResource Wcu.Brush.Surface.Input}" />
        <Setter Property="Foreground" Value="{DynamicResource Wcu.Brush.Accent.Default}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Wcu.Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="6" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ProgressBar}">
                    <Grid>
                        <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource Wcu.CornerRadius}" />
                        <Border x:Name="PART_Indicator"
                                HorizontalAlignment="Left"
                                Background="{TemplateBinding Foreground}"
                                CornerRadius="{DynamicResource Wcu.CornerRadius}"
                                Margin="1" />
                        <!-- 不確定モード: 幅に依存しないようグラデーション位置をアニメーション -->
                        <Border x:Name="IndeterminateBar"
                                CornerRadius="{DynamicResource Wcu.CornerRadius}"
                                Margin="1"
                                Visibility="Collapsed">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop x:Name="StopA" Color="Transparent" Offset="-0.6" />
                                    <GradientStop x:Name="StopB" Offset="-0.3"
                                                  Color="{Binding Foreground.Color, RelativeSource={RelativeSource TemplatedParent}}" />
                                    <GradientStop x:Name="StopC" Color="Transparent" Offset="0.0" />
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsIndeterminate" Value="True">
                            <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IndeterminateBar" Property="Visibility" Value="Visible" />
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="IndeterminateStoryboard">
                                    <Storyboard RepeatBehavior="Forever" Duration="0:0:1.6">
                                        <DoubleAnimation Storyboard.TargetName="StopA"
                                                         Storyboard.TargetProperty="Offset"
                                                         From="-0.6" To="1.0" Duration="0:0:1.6" />
                                        <DoubleAnimation Storyboard.TargetName="StopB"
                                                         Storyboard.TargetProperty="Offset"
                                                         From="-0.3" To="1.3" Duration="0:0:1.6" />
                                        <DoubleAnimation Storyboard.TargetName="StopC"
                                                         Storyboard.TargetProperty="Offset"
                                                         From="0.0" To="1.6" Duration="0:0:1.6" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="IndeterminateStoryboard" />
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ProgressBar}" BasedOn="{StaticResource Wcu.ProgressBar}" />

</ResourceDictionary>
