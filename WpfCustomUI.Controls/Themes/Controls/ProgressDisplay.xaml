<!-- 単一ジョブ進捗表示の既定スタイル(spec 6.6) -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wcu="clr-namespace:WpfCustomUI.Controls">

    <Style TargetType="{x:Type wcu:ProgressDisplay}">
        <Setter Property="Foreground" Value="{DynamicResource Wcu.Brush.Text.Primary}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type wcu:ProgressDisplay}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <DockPanel>
                            <TextBlock DockPanel.Dock="Right"
                                       Text="{TemplateBinding PercentText}"
                                       Foreground="{DynamicResource Wcu.Brush.Text.Secondary}"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center" />
                            <TextBlock Text="{TemplateBinding Text}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                        </DockPanel>

                        <ProgressBar Grid.Row="1"
                                     Height="8"
                                     Margin="0,6"
                                     Minimum="{TemplateBinding Minimum}"
                                     Maximum="{TemplateBinding Maximum}"
                                     Value="{TemplateBinding Value}"
                                     IsIndeterminate="{TemplateBinding IsIndeterminate}" />

                        <DockPanel Grid.Row="2">
                            <Button x:Name="CancelButton"
                                    DockPanel.Dock="Right"
                                    Content="{TemplateBinding CancelText}"
                                    Command="{TemplateBinding CancelCommand}"
                                    CommandParameter="{TemplateBinding CancelCommandParameter}"
                                    MinWidth="72" />
                            <TextBlock Text="{TemplateBinding ElapsedText}"
                                       Foreground="{DynamicResource Wcu.Brush.Text.Secondary}"
                                       VerticalAlignment="Center" />
                        </DockPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="CancelCommand" Value="{x:Null}">
                            <Setter TargetName="CancelButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
