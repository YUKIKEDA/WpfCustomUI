<!-- ログコンソールの既定スタイル(spec 6.5)。行は仮想化+リサイクルで大量ログに耐える -->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:wcu="clr-namespace:WpfCustomUI.Controls">

    <BooleanToVisibilityConverter x:Key="Wcu.LogConsole.BoolToVisibility" />

    <DataTemplate x:Key="Wcu.LogConsole.ItemTemplate" DataType="{x:Type wcu:LogEntry}">
        <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="TimestampText"
                       Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss.fff}'}"
                       Foreground="{DynamicResource Wcu.Brush.Text.Secondary}"
                       Margin="0,0,8,0"
                       Visibility="{Binding ShowTimestamps,
                                            RelativeSource={RelativeSource AncestorType={x:Type wcu:LogConsole}},
                                            Converter={StaticResource Wcu.LogConsole.BoolToVisibility}}" />
            <TextBlock x:Name="MessageText"
                       Text="{Binding Message}"
                       Foreground="{DynamicResource Wcu.Brush.Text.Primary}" />
        </StackPanel>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Level}" Value="Debug">
                <Setter TargetName="MessageText" Property="Foreground" Value="{DynamicResource Wcu.Brush.Text.Secondary}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Level}" Value="Warning">
                <Setter TargetName="MessageText" Property="Foreground" Value="{DynamicResource Wcu.Brush.Warning}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Level}" Value="Error">
                <Setter TargetName="MessageText" Property="Foreground" Value="{DynamicResource Wcu.Brush.Error}" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <Style x:Key="Wcu.LogConsole.ItemContainer" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border x:Name="Root"
                            Background="Transparent"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Wcu.Brush.State.Selected}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="Selector.IsSelectionActive" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource Wcu.Brush.State.SelectedInactive}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type wcu:LogConsole}">
        <Setter Property="Background" Value="{DynamicResource Wcu.Brush.Surface.Window}" />
        <Setter Property="Foreground" Value="{DynamicResource Wcu.Brush.Text.Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Wcu.Brush.Border.Default}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{DynamicResource Wcu.FontFamily.Monospace}" />
        <Setter Property="FontSize" Value="{DynamicResource Wcu.FontSize.S}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type wcu:LogConsole}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Wcu.CornerRadius}">
                        <ListBox x:Name="PART_List"
                                 ItemsSource="{Binding Source.Entries, RelativeSource={RelativeSource TemplatedParent}}"
                                 SelectionMode="Extended"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="0,2"
                                 Foreground="{TemplateBinding Foreground}"
                                 ItemContainerStyle="{StaticResource Wcu.LogConsole.ItemContainer}"
                                 ItemTemplate="{StaticResource Wcu.LogConsole.ItemTemplate}"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
